{% extends 'base.html.twig' %}

{% block title %}Nouvelle sortie{% endblock %}

{% block body %}
<div class="row">

    <div class="col-md-7">
        <br />
    <h1>Créer une sortie</h1>

    {{ include('sortie/_form.html.twig') }}

    <a href="{{ path('sortie_index') }}">Retour à la liste</a>
    </div>
    <div class="col-md-5">
        <img class="img-fluid" src="{{ asset('img/sortir.png') }}" alt="Créer une sortie">
    </div>
</div>
{% endblock %}
